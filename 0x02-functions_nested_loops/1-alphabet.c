#include <main.h>

/**
 * print_alphabets - Prints the alphabets from a to z in lower case
 * return void
 */
void print_alphabets(void)
{
    char let;

    for (let = 'a'; let <= 'z'; let++)
    {
        _putchar(letter);

    }

    _putchar('\n');
}

int main(void)
{
    print_alphabets();
    return (0);
}

