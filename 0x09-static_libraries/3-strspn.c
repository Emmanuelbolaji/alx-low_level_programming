#include <stdio.h>

/**
 * _strspn - function that gets length of prefix substring
 * @s: string that it's length will be checked
 * @accept: supporting character
 * Return: 0 always
 */

unsigned int _strspn(char *s, char *accept)
{
	return (0);
}
