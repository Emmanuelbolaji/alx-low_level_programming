#include <stdio.h>

/**
 * _strpbrk - function searches string for any of set of bytes
 * @s: string too be searched
 * @accept: supporting string
 * Return: 0 always
 */

char *_strpbrk(char *s, char *accept)
{
	return (0);
}
